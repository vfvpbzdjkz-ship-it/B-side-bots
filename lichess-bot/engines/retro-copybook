#!/usr/bin/env bash
# lichess-bot wrapper for the copybook engine. Runs the shared UCI driver with ENGINE=copybook.
set -euo pipefail
REPO="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$REPO"
exec env ENGINE=copybook python3 -m retro.uci "$@"

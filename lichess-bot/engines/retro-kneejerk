#!/usr/bin/env bash
# lichess-bot wrapper for the kneejerk engine. Runs the shared UCI driver with ENGINE=kneejerk.
set -euo pipefail
REPO="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$REPO"
exec env ENGINE=kneejerk python3 -m retro.uci "$@"

#!/usr/bin/env bash
# lichess-bot wrapper for the shannstein engine. Runs the shared UCI driver with ENGINE=shannstein.
set -euo pipefail
REPO="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$REPO"
exec env ENGINE=shannstein python3 -m retro.uci "$@"

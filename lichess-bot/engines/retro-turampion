#!/usr/bin/env bash
# lichess-bot wrapper for the turampion engine. Runs the shared UCI driver with ENGINE=turampion.
set -euo pipefail
REPO="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$REPO"
exec env ENGINE=turampion python3 -m retro.uci "$@"
